name: lingo_leap
description: A 2D language learning platformer game
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # To generate icons, run: flutter pub run flutter_launcher_icons
  # You'll need to add flutter_launcher_icons to dev_dependencies and configure it
